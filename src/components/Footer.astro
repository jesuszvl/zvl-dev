<footer>
  <div class='responsive-content footer-content'>
    <a href='/' class='logo'>
      zvl<span class='sign'>.</span>dev
    </a>
    <nav aria-label='Footer navigation'>
      <a href='mailto:jesus@zvl.dev'>Email</a>
      <a
        href='https://github.com/jesuszvl'
        target='_blank'
        rel='noopener noreferrer'>GitHub</a
      >
      <a
        href='https://www.linkedin.com/in/jesuszvl/'
        target='_blank'
        rel='noopener noreferrer'>LinkedIn</a
      >
    </nav>
    <p>© {new Date().getFullYear()} Jesús Zavala</p>
  </div>
</footer>

<style>
  footer {
    border-top: 1px solid var(--border);
  }

  .footer-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 28px;
    min-height: 96px;
  }

  .logo {
    color: var(--text);
    font-size: 20px;
    font-weight: 800;
  }

  .sign {
    color: var(--accent);
  }

  nav {
    display: flex;
    align-items: center;
    gap: 24px;
  }

  nav a {
    color: var(--muted);
    font-size: 14px;
    font-weight: 600;
  }

  nav a:hover {
    color: var(--text);
  }

  p {
    color: var(--subtle);
    font-size: 13px;
  }

  @media (max-width: 640px) {
    .footer-content {
      min-height: 0;
      flex-direction: column;
      padding: 32px 0;
    }
  }
</style>
